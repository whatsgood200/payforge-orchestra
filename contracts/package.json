{
  "name": "payforge-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network celoSepolia",
    "deploy:celo": "hardhat run scripts/deploy.ts --network celo",
    "register:sepolia": "hardhat run scripts/register-agents.ts --network celoSepolia",
    "register:celo": "hardhat run scripts/register-agents.ts --network celo",
    "verify:sepolia": "hardhat verify --network celoSepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0",
    "solc": "^0.8.35",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
